{
  "name": "god-of-small-things",
  "version": "0.2.0",
  "private": true,
  "description": "A living tabletop civilization simulator built with Three.js for integrated graphics.",
  "type": "module",
  "scripts": {
    "dev": "node server.mjs",
    "start": "node server.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:e2e": "playwright test",
    "check": "node --check src/bootstrap.js && node --check src/game.js && node --check src/fallback.js && node --check src/simulation.js && node --check server.mjs && npm test",
    "check:all": "npm run check && npm run test:e2e"
  },
  "keywords": [
    "threejs",
    "simulation",
    "god-game",
    "procedural-generation",
    "webgl",
    "browser-game"
  ],
  "devDependencies": {
    "@playwright/test": "1.62.0"
  },
  "license": "MIT",
  "engines": {
    "node": ">=20"
  }
}
